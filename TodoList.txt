FEATURES:

Big int for better probability ranges
Adding probability gate to output(input?).
Proper configuration
Unit tests?
Nested if checks?
Assignment between registers


BUGS:

CONSIDERATIONS:

Emoji would be intereting. Requires an editor to map emoji to ascii.
Three variable storage OR Reduced maxc array, each element has a register for comparison.